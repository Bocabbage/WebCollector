// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"news-subscriber-bff/internal/data/ent/articlemeta"
	"news-subscriber-bff/internal/data/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ArticleMetaQuery is the builder for querying ArticleMeta entities.
type ArticleMetaQuery struct {
	config
	ctx        *QueryContext
	order      []articlemeta.OrderOption
	inters     []Interceptor
	predicates []predicate.ArticleMeta
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ArticleMetaQuery builder.
func (amq *ArticleMetaQuery) Where(ps ...predicate.ArticleMeta) *ArticleMetaQuery {
	amq.predicates = append(amq.predicates, ps...)
	return amq
}

// Limit the number of records to be returned by this query.
func (amq *ArticleMetaQuery) Limit(limit int) *ArticleMetaQuery {
	amq.ctx.Limit = &limit
	return amq
}

// Offset to start from.
func (amq *ArticleMetaQuery) Offset(offset int) *ArticleMetaQuery {
	amq.ctx.Offset = &offset
	return amq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (amq *ArticleMetaQuery) Unique(unique bool) *ArticleMetaQuery {
	amq.ctx.Unique = &unique
	return amq
}

// Order specifies how the records should be ordered.
func (amq *ArticleMetaQuery) Order(o ...articlemeta.OrderOption) *ArticleMetaQuery {
	amq.order = append(amq.order, o...)
	return amq
}

// First returns the first ArticleMeta entity from the query.
// Returns a *NotFoundError when no ArticleMeta was found.
func (amq *ArticleMetaQuery) First(ctx context.Context) (*ArticleMeta, error) {
	nodes, err := amq.Limit(1).All(setContextOp(ctx, amq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{articlemeta.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (amq *ArticleMetaQuery) FirstX(ctx context.Context) *ArticleMeta {
	node, err := amq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ArticleMeta ID from the query.
// Returns a *NotFoundError when no ArticleMeta ID was found.
func (amq *ArticleMetaQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = amq.Limit(1).IDs(setContextOp(ctx, amq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{articlemeta.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (amq *ArticleMetaQuery) FirstIDX(ctx context.Context) int {
	id, err := amq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ArticleMeta entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ArticleMeta entity is found.
// Returns a *NotFoundError when no ArticleMeta entities are found.
func (amq *ArticleMetaQuery) Only(ctx context.Context) (*ArticleMeta, error) {
	nodes, err := amq.Limit(2).All(setContextOp(ctx, amq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{articlemeta.Label}
	default:
		return nil, &NotSingularError{articlemeta.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (amq *ArticleMetaQuery) OnlyX(ctx context.Context) *ArticleMeta {
	node, err := amq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ArticleMeta ID in the query.
// Returns a *NotSingularError when more than one ArticleMeta ID is found.
// Returns a *NotFoundError when no entities are found.
func (amq *ArticleMetaQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = amq.Limit(2).IDs(setContextOp(ctx, amq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{articlemeta.Label}
	default:
		err = &NotSingularError{articlemeta.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (amq *ArticleMetaQuery) OnlyIDX(ctx context.Context) int {
	id, err := amq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ArticleMetaSlice.
func (amq *ArticleMetaQuery) All(ctx context.Context) ([]*ArticleMeta, error) {
	ctx = setContextOp(ctx, amq.ctx, "All")
	if err := amq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ArticleMeta, *ArticleMetaQuery]()
	return withInterceptors[[]*ArticleMeta](ctx, amq, qr, amq.inters)
}

// AllX is like All, but panics if an error occurs.
func (amq *ArticleMetaQuery) AllX(ctx context.Context) []*ArticleMeta {
	nodes, err := amq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ArticleMeta IDs.
func (amq *ArticleMetaQuery) IDs(ctx context.Context) (ids []int, err error) {
	if amq.ctx.Unique == nil && amq.path != nil {
		amq.Unique(true)
	}
	ctx = setContextOp(ctx, amq.ctx, "IDs")
	if err = amq.Select(articlemeta.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (amq *ArticleMetaQuery) IDsX(ctx context.Context) []int {
	ids, err := amq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (amq *ArticleMetaQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, amq.ctx, "Count")
	if err := amq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, amq, querierCount[*ArticleMetaQuery](), amq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (amq *ArticleMetaQuery) CountX(ctx context.Context) int {
	count, err := amq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (amq *ArticleMetaQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, amq.ctx, "Exist")
	switch _, err := amq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (amq *ArticleMetaQuery) ExistX(ctx context.Context) bool {
	exist, err := amq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ArticleMetaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (amq *ArticleMetaQuery) Clone() *ArticleMetaQuery {
	if amq == nil {
		return nil
	}
	return &ArticleMetaQuery{
		config:     amq.config,
		ctx:        amq.ctx.Clone(),
		order:      append([]articlemeta.OrderOption{}, amq.order...),
		inters:     append([]Interceptor{}, amq.inters...),
		predicates: append([]predicate.ArticleMeta{}, amq.predicates...),
		// clone intermediate query.
		sql:  amq.sql.Clone(),
		path: amq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UID int64 `json:"uid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ArticleMeta.Query().
//		GroupBy(articlemeta.FieldUID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (amq *ArticleMetaQuery) GroupBy(field string, fields ...string) *ArticleMetaGroupBy {
	amq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ArticleMetaGroupBy{build: amq}
	grbuild.flds = &amq.ctx.Fields
	grbuild.label = articlemeta.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UID int64 `json:"uid,omitempty"`
//	}
//
//	client.ArticleMeta.Query().
//		Select(articlemeta.FieldUID).
//		Scan(ctx, &v)
func (amq *ArticleMetaQuery) Select(fields ...string) *ArticleMetaSelect {
	amq.ctx.Fields = append(amq.ctx.Fields, fields...)
	sbuild := &ArticleMetaSelect{ArticleMetaQuery: amq}
	sbuild.label = articlemeta.Label
	sbuild.flds, sbuild.scan = &amq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ArticleMetaSelect configured with the given aggregations.
func (amq *ArticleMetaQuery) Aggregate(fns ...AggregateFunc) *ArticleMetaSelect {
	return amq.Select().Aggregate(fns...)
}

func (amq *ArticleMetaQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range amq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, amq); err != nil {
				return err
			}
		}
	}
	for _, f := range amq.ctx.Fields {
		if !articlemeta.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if amq.path != nil {
		prev, err := amq.path(ctx)
		if err != nil {
			return err
		}
		amq.sql = prev
	}
	return nil
}

func (amq *ArticleMetaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ArticleMeta, error) {
	var (
		nodes = []*ArticleMeta{}
		_spec = amq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ArticleMeta).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ArticleMeta{config: amq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, amq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (amq *ArticleMetaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := amq.querySpec()
	_spec.Node.Columns = amq.ctx.Fields
	if len(amq.ctx.Fields) > 0 {
		_spec.Unique = amq.ctx.Unique != nil && *amq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, amq.driver, _spec)
}

func (amq *ArticleMetaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(articlemeta.Table, articlemeta.Columns, sqlgraph.NewFieldSpec(articlemeta.FieldID, field.TypeInt))
	_spec.From = amq.sql
	if unique := amq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if amq.path != nil {
		_spec.Unique = true
	}
	if fields := amq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, articlemeta.FieldID)
		for i := range fields {
			if fields[i] != articlemeta.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := amq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := amq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := amq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := amq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (amq *ArticleMetaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(amq.driver.Dialect())
	t1 := builder.Table(articlemeta.Table)
	columns := amq.ctx.Fields
	if len(columns) == 0 {
		columns = articlemeta.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if amq.sql != nil {
		selector = amq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if amq.ctx.Unique != nil && *amq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range amq.predicates {
		p(selector)
	}
	for _, p := range amq.order {
		p(selector)
	}
	if offset := amq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := amq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ArticleMetaGroupBy is the group-by builder for ArticleMeta entities.
type ArticleMetaGroupBy struct {
	selector
	build *ArticleMetaQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (amgb *ArticleMetaGroupBy) Aggregate(fns ...AggregateFunc) *ArticleMetaGroupBy {
	amgb.fns = append(amgb.fns, fns...)
	return amgb
}

// Scan applies the selector query and scans the result into the given value.
func (amgb *ArticleMetaGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, amgb.build.ctx, "GroupBy")
	if err := amgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ArticleMetaQuery, *ArticleMetaGroupBy](ctx, amgb.build, amgb, amgb.build.inters, v)
}

func (amgb *ArticleMetaGroupBy) sqlScan(ctx context.Context, root *ArticleMetaQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(amgb.fns))
	for _, fn := range amgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*amgb.flds)+len(amgb.fns))
		for _, f := range *amgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*amgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := amgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ArticleMetaSelect is the builder for selecting fields of ArticleMeta entities.
type ArticleMetaSelect struct {
	*ArticleMetaQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ams *ArticleMetaSelect) Aggregate(fns ...AggregateFunc) *ArticleMetaSelect {
	ams.fns = append(ams.fns, fns...)
	return ams
}

// Scan applies the selector query and scans the result into the given value.
func (ams *ArticleMetaSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ams.ctx, "Select")
	if err := ams.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ArticleMetaQuery, *ArticleMetaSelect](ctx, ams.ArticleMetaQuery, ams, ams.inters, v)
}

func (ams *ArticleMetaSelect) sqlScan(ctx context.Context, root *ArticleMetaQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ams.fns))
	for _, fn := range ams.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ams.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ams.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
